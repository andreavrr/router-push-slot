import React, { type ReactNode } from "react";
import {ThemedText} from "@/components/ThemedText";

const Page = (): ReactNode => {
    return <ThemedText>Details</ThemedText>;
};

export default Page;
